# Yollr Beast™ Environment Variables
# Copy this file to .env.local and fill in your actual values

# ==========================================
# UAT Mode Configuration
# ==========================================
# Set to 'true' to use MOCK_DATA only (no Firebase queries)
# This is useful for local development without Firebase
# IMPORTANT: Set to 'false' or remove for production deployment
NEXT_PUBLIC_UAT_MODE=true

# ==========================================
# Demo Mode Configuration
# ==========================================
# Set to 'true' for simple email/password authentication (no external auth service)
# Uses localStorage-based authentication with demo accounts
# See DEMO_ACCOUNTS.md for test credentials (5 universities)
# IMPORTANT: Only use for local testing/demos, NEVER in production
NEXT_PUBLIC_DEMO_MODE=true

# ==========================================
# App Configuration
# ==========================================
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=Yollr Beast

# ==========================================
# Firebase Configuration (Optional in demo mode)
# ==========================================
# Get your Firebase config from: https://console.firebase.google.com/
# Only required if NEXT_PUBLIC_UAT_MODE=false
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# ==========================================
# Vercel Deployment Instructions
# ==========================================
# 1. Go to your Vercel project settings
# 2. Navigate to "Environment Variables"
# 3. Add ALL variables above (both NEXT_PUBLIC_* and non-prefixed)
# 4. IMPORTANT: Add variables to all environments (Production, Preview, Development)
# 5. Redeploy after adding variables
#
# Required variables for Vercel:
# - NEXT_PUBLIC_APP_URL (your production URL)
# - All Firebase variables (if not using UAT_MODE)
# - NEXT_PUBLIC_UAT_MODE (set to 'false' for production)
# - NEXT_PUBLIC_DEMO_MODE (set to 'false' for production)
